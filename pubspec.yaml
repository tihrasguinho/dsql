name: dsql
description: Dart SQL library for generating .dart files from .sql files
version: 1.0.2
homepage: https://github.com/tihrasguinho/dsql
topics:
  - postgres
  - orm

executables: 
  dsql: dsql

environment:
  sdk: ^3.0.0

license: MIT

dependencies:
  args: ^2.5.0
  path: ^1.8.3
  postgres: ^3.2.1
  strings: ^3.1.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
